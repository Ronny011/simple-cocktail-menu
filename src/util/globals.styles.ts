import styled from 'styled-components';

export const Button = styled.button`
  color: inherit;
  background: none;
  border: none;
  width: 150px;
  font-weight: bold;
  font-size: 15px;
  padding: 15px 0 15px 0;
  cursor: pointer;
  font-family: inherit;
`;
